# Frontend

React + TypeScript + Vite prototype for Ziggy Intelligence. The UI currently uses isolated mock data and an Axios service seam; live API and ML integrations are intentionally deferred.

Run `npm install` and `npm run dev`. Build with `npm run build`.
