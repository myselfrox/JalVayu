import ForecastChart from '../components/ForecastChart'; import { mockData } from '../data/mockWeather';
export default function Analytics(){return <main className="page"><div className="eyebrow">Signal analytics</div><h1 className="title">Forecast analytics</h1><p className="muted">Scientific comparison views for forecast departure and anomaly intensity.</p><div className="grid stats section">{[['Detected anomalies','18'],['Extreme events','4'],['Highest score','-0.74'],['Average confidence','84%']].map(([a,b])=><div className="panel" key={a}><span className="stat-label">{a}</span><div className="stat-value">{b}</div></div>)}</div><section className="panel section"><div className="eyebrow">Temperature signal</div><h2>Forecast vs historical baseline</h2><ForecastChart data={mockData.forecast}/></section><div className="grid two-col section">{['Temperature anomaly','Rainfall anomaly','Wind anomaly','Anomaly intensity'].map((title,i)=><section className="panel" key={title}><h3>{title}</h3><ForecastChart data={mockData.forecast.map(p=>({...p,temperature:p.anomaly.temperature+(i*3)}))}/></section>)}</div></main>}
