import { ExtremeEvent } from '../types/weather';
export default function EventCard({event,onSelect}:{event:ExtremeEvent;onSelect?:()=>void}){return <article className="event-card" onClick={onSelect} onKeyDown={e=>e.key==='Enter'&&onSelect?.()} role={onSelect?'button':undefined} tabIndex={onSelect?0:undefined}><div className="event-meta"><span>{event.startTime}</span><span className={`risk risk-${event.severity.toLowerCase()}`}>{event.severity}</span></div><h3>{event.type} · {event.location.name}</h3><div className="event-meta"><span>{event.duration}</span><b>{event.magnitude}</b></div><p className="muted" style={{fontSize:12,lineHeight:1.5}}>{event.explanation}</p></article>}
