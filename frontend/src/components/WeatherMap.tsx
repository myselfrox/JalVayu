import { ForecastPoint } from '../types/weather';
import MapLegend, { Marker } from './MapLegend';
interface Props{point:ForecastPoint; onLocation:(name:string)=>void}
export default function WeatherMap({point,onLocation}:Props){const pins=[{x:30,y:38},{x:53,y:47},{x:42,y:66},{x:67,y:30}];return <section className="panel map-wrap"><div className="map-canvas"><div className="map-title"><div className="eyebrow">Spatio-temporal field</div><strong>{point.step} · {point.location.name}</strong></div><div className="map-grid"/>{pins.map((pin,i)=><Marker key={i} location={point.location} risk={i===0?point.ai.risk_level:'WATCH'} left={pin.x} top={pin.y} onSelect={()=>onLocation(point.location.name)}/>) }<MapLegend/><div className="map-fallback"><div><Activity size={35} color="#45aaf7"/><p>MapLibre-ready prototype surface</p><small className="muted">Simulated geographic anomaly markers. Connect a style URL in the map adapter when credentials are available.</small></div></div></div></section>}
