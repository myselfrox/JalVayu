import { Pause, Play, SkipBack, SkipForward } from 'lucide-react';
interface Props{index:number;playing:boolean;onIndex:(n:number)=>void;onPlaying:(v:boolean)=>void;steps:string[]}
export default function TimeSlider({index,playing,onIndex,onPlaying,steps}:Props){return <section className="panel section"><div className="section-head"><div><div className="eyebrow">Spatio-temporal time machine</div><h2 style={{margin:'5px 0'}}>Anomaly evolution</h2></div><span className="muted">{steps[index]}</span></div><div className="timeline"><button className="icon-btn" aria-label="Previous timestep" onClick={()=>onIndex(Math.max(0,index-1))}><SkipBack size={16}/></button><input aria-label="Forecast timestep" type="range" min="0" max={steps.length-1} value={index} onChange={e=>onIndex(Number(e.target.value))}/><button className="icon-btn" aria-label="Next timestep" onClick={()=>onIndex(Math.min(steps.length-1,index+1))}><SkipForward size={16}/></button><button className="primary-btn" onClick={()=>onPlaying(!playing)}>{playing?<Pause size={15}/>:<Play size={15}/>} {playing?'Pause':'Play'}</button></div><div className="timeline-steps">{steps.map(s=><span key={s}>{s}</span>)}</div></section>}
