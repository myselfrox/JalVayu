import { ForecastPoint, Variable } from '../types/weather';
const labels:{keyof Pick<ForecastPoint,'temperature'|'rainfall'|'wind_speed'|'apparent_temperature'>:string}={temperature:'Temperature',rainfall:'Rainfall',wind_speed:'Wind',apparent_temperature:'Heat'};
export default function WeatherStats({point}:{point:ForecastPoint}){const items=[['temperature',point.temperature,'°C',point.anomaly.temperature],['rainfall',point.rainfall,'mm',point.anomaly.rainfall],['wind_speed',point.wind_speed,'km/h',point.anomaly.wind],['apparent_temperature',point.apparent_temperature,'°C',point.anomaly.heat]] as const;return <div className="grid stats section">{items.map(([key,value,unit,anomaly])=><div className="panel" key={key}><span className="stat-label">{labels[key]}</span><div className="stat-value">{value.toFixed(1)} <small>{unit}</small></div><div className="metric">Anomaly <b style={{color:'#ffaf70'}}>+{anomaly}{key==='rainfall'||key==='wind_speed'?'%':'°'}</b></div><span className="risk risk-high">{point.ai.risk_level}</span></div>)}</div>}
