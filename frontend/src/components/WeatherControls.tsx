import { Variable } from '../types/weather';
interface Props{variable:Variable; region:string; duration:number; onVariable:(v:Variable)=>void; onRegion:(v:string)=>void; onDuration:(v:number)=>void}
export default function WeatherControls(p:Props){return <aside className="panel"><div className="eyebrow">Controls</div><h3>Forecast lens</h3><div className="control"><label htmlFor="variable">Weather variable</label><select id="variable" value={p.variable} onChange={e=>p.onVariable(e.target.value as Variable)}>{['temperature','rainfall','wind','heat'].map(v=><option key={v} value={v}>{v[0].toUpperCase()+v.slice(1)}</option>)}</select></div><div className="control"><label htmlFor="region">Region</label><select id="region" value={p.region} onChange={e=>p.onRegion(e.target.value)}>{['All regions','Punjab','Delhi NCR','Rajasthan','Assam'].map(v=><option key={v}>{v}</option>)}</select></div><div className="control"><label htmlFor="duration">Forecast window</label><select id="duration" value={p.duration} onChange={e=>p.onDuration(Number(e.target.value))}>{[24,48,72,96,120].map(v=><option key={v} value={v}>{v} hours</option>)}</select></div><p className="muted" style={{fontSize:12,lineHeight:1.6}}>Change the lens to explore how simulated anomalies evolve across the forecast window.</p></aside>}
